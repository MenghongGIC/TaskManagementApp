package com.taskmanager.model;
@SuppressWarnings("unused")

public class Team {
    private int id;                    // → teams.id
    private String name;               // → teams.name
    private int workspaceId;           // → teams.workspace_id
    private Integer managerId;         // → teams.manager_id (Integer = can be null)

    // constructor + getters/setters
}